import matplotlib.pyplot as plt

# Example quick plot to use `plt` and silence unused-import warnings
plt.plot([0,1,2], [0,1,0])
plt.title("Test plot")
plt.close()   # close if you don't want a window during quick checks
